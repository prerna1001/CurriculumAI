# One action agreement

Fill this in together during readiness. These values are configuration metadata, not secrets.

| Field | Agreed value |
| --- | --- |
| One platform | Pending |
| Action ID | Pending |
| Connection key | Pending |
| Output format / content type | Pending |
| Test destination | Pending |

Each developer keeps `ONE_SECRET` only in their own local `.env` file.
